# For development: point to local backend
VITE_API_URL=http://localhost:5001

# For production: comment out above and the api.config.js will auto-detect
# production domain based on window.location.protocol

